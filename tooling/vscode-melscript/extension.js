const vscode = require('vscode');

function activate(context) {
    console.log('MelScript extension is now active!');
    // Nenhuma lógica adicional necessária por enquanto, apenas syntax highlighting
}

function deactivate() {}

module.exports = {
    activate,
    deactivate
};
